run:
  name: qp_mpc_cuopt_1e-6
  output_dir: results
  parallelism: 1
  resume: true
  timeout_seconds: 360.0
  datasets:
  - name: mpc_qpbenchmark
    id: mpc
    dataset_options:
      subset: all
solvers:
- id: cuopt_1e-6
  solver: cuopt
  settings:
    eps: 1.0e-06
    time_limit: 300.0
